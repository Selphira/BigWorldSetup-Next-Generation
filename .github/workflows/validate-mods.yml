name: Validate Mod Descriptions

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'data/mods/**.json'
      - 'validation/schemas/mod-definition.json'
      - 'validation/validator.py'
      - '.github/workflows/validate-mods.yml'

  pull_request:
    branches: [ main, develop ]
    paths:
      - 'data/mods/**.json'
      - 'validation/schemas/mod-definition.json'
      - 'validation/validator.py'

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate modified mod JSON files

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install jsonschema

      - name: Get changed JSON files
        id: changed
        uses: tj-actions/changed-files@v47
        with:
          separator: ","
          files: |
            data/mods/**.json

      - name: Validate modified JSON files
        id: validate
        if: steps.changed.outputs.all_changed_files != ''
        shell: bash {0}
        run: |
          echo "🔍 Validating modified files..."
          
          # Lire la sortie multiligne dans un tableau bash
          IFS=$',' read -a changed_files <<< "${{ steps.changed.outputs.modified_files }}"
          
          results=""
          errors=""
          
          for file in "${changed_files[@]}"; do
            echo "-------------------------------------"
            echo "Validating $file..."
            output=$(python validation/validator.py "$file" validation/schemas/mod-definition.json 2>&1)
            status=$?
          
            if [ $status -ne 0 ]; then
              errors="$errors\n❌ **$file**\n\`\`\`\n$output\n\`\`\`\n"
            else
              results="$results\n✅ $file passed validation."
            fi
          done
          
          if [ -n "$errors" ]; then
            echo "-------------------------------------"
            echo -e "❌ Validation errors found."
            echo -e "$errors"
            echo "-------------------------------------"
            {
              echo "error_report<<EOF"
              echo -e "$errors"
              echo "EOF"
            } >> $GITHUB_OUTPUT
            exit 1
          else
            echo "✅ All modified JSON files are valid."
          fi

      - name: Skip validation (no JSON changes)
        if: steps.changed.outputs.all_changed_files == ''
        run: echo "✅ No modified JSON files to validate."

      - name: Comment validation results on PR
        if: failure() && github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: json-validation
          message: |
            ⚠️ **JSON Validation Failed**
            
            The following files did not pass validation:
            
            ${{ steps.validate.outputs.error_report }}
            
            Please fix the above issues and push again to re-run validation.
